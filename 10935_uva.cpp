#include <bits/stdc++.h>

using namespace std;

main() {

    queue<int> deck;

    int n;

    while (cin >> n) {

        for (int i=n; i>0; i--) {
            deck.push(i);
        }

        while (deck.size() > 2) {
            deck.pop();
            deck.
        }
    }


}
